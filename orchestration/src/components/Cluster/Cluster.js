import React from 'react';
import './Cluster.css';

const Cluster = (props) => {

    return (
        <div className="Cluster">
            {props.cluster}
        </div>
    );


}

export default Cluster;
